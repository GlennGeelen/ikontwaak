<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>IkOntwaak.nu - <%= current_page.data.title %></title>
    <%= stylesheet_link_tag "main" %>
  </head>
  <body class="<%= current_page.data.body_class %>">
    <header class="header">
      <div class="header-wrap">
        <%= image_tag "SH4U_logo.png", :class => "logo", :alt => "Ik ontwaak.nu logo" %>
        <span class="title">Ik Ontwaak.nu</span>
      </div>
      <nav class="nav">
        <% sitemap.resources.delete_if{|i| i.data.order == nil}.sort_by!{|a| a.data.order}.each do |f| %>
          <% if f.content_type.include?("html") %>
            <a class="nav-item <%= current_page.path == f.path ? "active" : "" %>" href="<%= f.url %>">
              <%= f.data.title %>
            </a>
          <% end %>
        <% end %>
      </nav>
    </header>
    <main role="main" class="main">
      <%= yield %>
    </main>
    <%= partial "partials/analytics" %>
  </body>
</html>
